name: R-hub

# Cancel older runs on the same branch to save minutes
concurrency:
  group: rhub-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
  pull_request:
  workflow_dispatch:
    inputs:
      config:
        description: "Comma-separated R-hub platforms"
        type: string
        default: "fedora-clang-devel,ubuntu-gcc-release,windows-x86_64-devel"
      name:
        description: "Run name (optional)"
        type: string
      id:
        description: "Unique ID (optional)"
        type: string

run-name: >-
  ${{ github.event.inputs.id }}:
  ${{ github.event.inputs.name || format('Triggered by {0}', github.triggering_actor) }}

jobs:
  setup:
    runs-on: ubuntu-latest
    outputs:
      containers: ${{ steps.rhub-setup.outputs.containers }}
      platforms:  ${{ steps.rhub-setup.outputs.platforms }}
    steps:
      # Determine which runners are needed for our platform list
      - uses: r-hub/actions/setup@v1
        id: rhub-setup
        with:
          # As documented by R-hub, these are the closest default platforms for
          # CRAN submission checks: Fedora clang devel, Ubuntu GCC release, and
          # Windows x86_64 devel. Sanitizer-only checks remain opt-in because
          # upstream R-devel dependency breakage is currently causing false CI failures.
          config: ${{ github.event.inputs.config || 'fedora-clang-devel,ubuntu-gcc-release,windows-x86_64-devel' }}

  linux-containers:
    needs: setup
    if: ${{ needs.setup.outputs.containers != '[]' }}
    runs-on: ubuntu-latest
    name: ${{ matrix.config.label }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJson(needs.setup.outputs.containers) }}
    container:
      image: ${{ matrix.config.container }}
    steps:
      - uses: r-hub/actions/checkout@v1
      - uses: r-hub/actions/platform-info@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - name: Remove stale pak lockfile
        run: rm -f .github/pkg.lock
        shell: bash
      - uses: r-hub/actions/setup-deps@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - uses: r-hub/actions/run-check@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}

  other-platforms:
    needs: setup
    if: ${{ needs.setup.outputs.platforms != '[]' }}
    runs-on: ${{ matrix.config.os }}
    name: ${{ matrix.config.label }}
    strategy:
      fail-fast: false
      matrix:
        config: ${{ fromJson(needs.setup.outputs.platforms) }}
    steps:
      - uses: r-hub/actions/checkout@v1
      - uses: r-hub/actions/setup-r@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - uses: r-hub/actions/platform-info@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - name: Remove stale pak lockfile
        run: rm -f .github/pkg.lock
        shell: bash
      - uses: r-hub/actions/setup-deps@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
      - uses: r-hub/actions/run-check@v1
        with:
          token: ${{ secrets.RHUB_TOKEN }}
          job-config: ${{ matrix.config.job-config }}
