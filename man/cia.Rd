% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cia.R
\name{cia}
\alias{cia}
\alias{summary.cia}
\alias{print.cia}
\alias{print.summary.cia}
\alias{plot.cia}
\alias{print.cia_ci}
\title{Coefficient of Individual Agreement}
\usage{
cia(
  data,
  response,
  subject,
  method,
  replicate,
  reference = NULL,
  scope = c("overall", "pairwise"),
  estimator = c("vc_constrained", "mom_unconstrained"),
  ci = FALSE,
  conf_level = 0.95,
  inference = c("bootstrap", "none"),
  B = 1000L,
  seed = NULL,
  n_threads = getOption("matrixCorr.threads", 1L),
  verbose = FALSE
)

\method{summary}{cia}(object, digits = 4, ci_digits = 3, ...)

\method{print}{cia}(
  x,
  digits = 4,
  n = NULL,
  topn = NULL,
  max_vars = NULL,
  width = NULL,
  show_ci = NULL,
  ...
)

\method{print}{summary.cia}(
  x,
  digits = NULL,
  n = NULL,
  topn = NULL,
  max_vars = NULL,
  width = NULL,
  show_ci = NULL,
  ...
)

\method{plot}{cia}(
  x,
  title = NULL,
  low_color = "indianred1",
  high_color = "steelblue1",
  mid_color = "white",
  value_text_size = 4,
  ci_text_size = 3,
  show_value = TRUE,
  ...
)

\method{print}{cia_ci}(x, ...)
}
\arguments{
\item{data}{A data frame in long format.}

\item{response}{Character scalar naming the numeric measurement column.}

\item{subject}{Character scalar naming the subject/unit identifier column.}

\item{method}{Character scalar naming the method/device/rater column.}

\item{replicate}{Character scalar naming the replicate identifier within each
subject-method cell.}

\item{reference}{Optional character scalar naming the reference method. When
\code{NULL} (default), the no-reference CIA is estimated.}

\item{scope}{One of \code{"overall"} or \code{"pairwise"}. \code{"overall"} returns one CIA
coefficient across all retained methods. \code{"pairwise"} returns the two-method
CIA for each method pair, or the reference-scaled CIA for each
method-versus-reference comparison when \code{reference} is supplied.}

\item{estimator}{One of \code{"vc_constrained"} or \code{"mom_unconstrained"}.
\code{"vc_constrained"} (default) reports CIA after constraining the estimated
inter-method variance component to be non-negative. \code{"mom_unconstrained"}
reports the raw method-of-moments ratio and may exceed 1.}

\item{ci}{Logical; if \code{TRUE}, attach subject-bootstrap percentile confidence
intervals.}

\item{conf_level}{Confidence level used when \code{ci = TRUE}. Default is \code{0.95}.}

\item{inference}{One of \code{"bootstrap"} or \code{"none"}.}

\item{B}{Number of subject-bootstrap resamples when \code{ci = TRUE} and
\code{inference = "bootstrap"}.}

\item{seed}{Optional positive integer seed for reproducible bootstrap
resampling.}

\item{n_threads}{Integer >= 1. Number of OpenMP threads passed to the C++
backend.}

\item{verbose}{Logical; if \code{TRUE}, emit short progress messages.}

\item{object}{A \code{cia} object.}

\item{digits}{Integer; number of decimal places for estimates.}

\item{ci_digits}{Integer; number of decimal places for confidence limits.}

\item{...}{Additional arguments passed to downstream print helpers.}

\item{x}{A \code{cia} object.}

\item{n}{Optional preview row threshold.}

\item{topn}{Optional number of leading/trailing rows to show when truncated.}

\item{max_vars}{Optional maximum number of visible columns.}

\item{width}{Optional display width.}

\item{show_ci}{One of \code{"yes"} or \code{"no"}.}

\item{title}{Optional plot title.}

\item{low_color}{Color used for lower agreement values.}

\item{high_color}{Color used for higher agreement values.}

\item{mid_color}{Midpoint color for pairwise heatmaps.}

\item{value_text_size}{Text size for overlaid estimate labels.}

\item{ci_text_size}{Text size for CI labels.}

\item{show_value}{Logical; whether to overlay numeric values.}
}
\value{
For \code{scope = "overall"}, a one-row data frame with class
\code{c("cia_overall", "cia", "data.frame")}. For \code{scope = "pairwise"} and
\code{ci = FALSE}, a dense matrix-style object using the package's standard
correlation-result infrastructure. For \code{scope = "pairwise"} and \code{ci = TRUE},
a list with elements \code{est}, \code{lwr.ci}, and \code{upr.ci}, classed as
\code{c("cia", "cia_ci")}.
}
\description{
\code{cia()} estimates the coefficient of individual agreement proposed by
Barnhart, Kosinski, and Haber. CIA assesses individual-level
interchangeability by comparing between-method disagreement with
within-method replicate disagreement. Unlike CCC, CIA is not intended to be
driven by between-subject heterogeneity.

The estimator requires replicated readings within method. A data set with
one observation per subject per method is insufficient for CIA because the
within-method disagreement term cannot be estimated.
}
\details{
Let \code{Y_ijk} denote replicate \code{k} on subject \code{i} measured by method \code{j}.

Without a reference method, CIA is defined by
\deqn{
\mathrm{CIA}^N =
\frac{\sum_j E\{(Y_{ijk} - Y_{ijk'})^2\} / 2}
     {\sum_{j < j'} E\{(Y_{ij} - Y_{ij'})^2\} / (J - 1)}.
}

With a reference method \code{J}, CIA is defined by
\deqn{
\mathrm{CIA}^R =
\frac{E\{(Y_{iJk} - Y_{iJk'})^2\}}
     {\sum_{j=1}^{J-1} E\{(Y_{ij} - Y_{iJ})^2\} / (J - 1)}.
}

This implementation estimates the required expectations by method of moments:
the within-method terms are averages of squared differences across all
distinct replicate pairs within each subject-method cell, and the
between-method terms are averages of squared differences across all
cross-method replicate combinations within subject.

Two estimators are available. The raw method-of-moments estimator keeps
\eqn{\hat{\tau}^2_{\mathrm{raw}} = \widehat{D} - \widehat{\sigma}^2_W}
unchanged and may therefore exceed 1 when \eqn{\hat{\tau}^2_{\mathrm{raw}} <
0}. The default variance-component-constrained estimator sets
\eqn{\hat{\tau}^2 = \max(\hat{\tau}^2_{\mathrm{raw}}, 0)} and reports
\eqn{\widehat{\mathrm{CIA}} = \widehat{\sigma}^2_W / (\hat{\tau}^2 +
\widehat{\sigma}^2_W)}. This applies the boundary on the implied
inter-method variance component rather than clamping CIA directly.

For \code{scope = "pairwise"} and \code{reference = NULL}, each off-diagonal entry is
based on the two-method special case. For methods \code{j} and \verb{j'}, let
\deqn{
\hat{\sigma}_{jj'}^2 = \frac{W_j + W_{j'}}{2}
}
and
\deqn{
\hat{\tau}_{jj',\mathrm{raw}}^2 = D_{jj'} - \hat{\sigma}_{jj'}^2.
}
The raw pairwise method-of-moments estimator is
\deqn{
\mathrm{CIA}^{\mathrm{raw}}_{jj'} =
\frac{\hat{\sigma}_{jj'}^2}{D_{jj'}}.
}
Under \code{estimator = "vc_constrained"}, the reported pairwise CIA is
\deqn{
\mathrm{CIA}_{jj'} = \frac{\hat{\sigma}_{jj'}^2}
{\hat{\sigma}_{jj'}^2 + \max(\hat{\tau}_{jj',\mathrm{raw}}^2, 0)},
}
while \code{estimator = "mom_unconstrained"} reports
\eqn{\mathrm{CIA}^{\mathrm{raw}}_{jj'}} directly. \code{W_j} is the within-method
mean squared replicate disagreement for method \code{j}, and \verb{D_\{jj'\}} is the
between-method mean squared disagreement for methods \code{j} and \verb{j'}.

For \code{scope = "pairwise"} with a reference method \code{R}, each estimable
method-versus-reference entry is
\deqn{
\mathrm{CIA}_{jR} = \frac{W_R}{D_{jR}}.
}

High CIA indicates stronger individual agreement. The FDA individual
bioequivalence boundary \code{IEC <= 2.4948} corresponds to \code{CIA >= 0.445}, and
\code{CIA >= 0.8} is sometimes used as a stronger practical rule. Such thresholds
are context-dependent and are not hard-coded by this function.

Missing rows in the required columns are removed before estimation and the
counts are recorded in \code{attr(x, "diagnostics")}.
}
\examples{
set.seed(1)
subjects <- sprintf("s\%02d", 1:30)
methods <- c("A", "B", "C")
replicates <- sprintf("r\%02d", 1:20)
dat <- expand.grid(
  subject = subjects,
  method = methods,
  replicate = replicates,
  KEEP.OUT.ATTRS = FALSE,
  stringsAsFactors = FALSE
)
subject_effect <- stats::rnorm(length(subjects), sd = 3)
method_shift <- c(A = 0, B = 0.15, C = -0.10)
method_sd <- c(A = 0.45, B = 0.45, C = 0.30)
dat$value <- subject_effect[match(dat$subject, subjects)] +
  method_shift[dat$method] +
  stats::rnorm(nrow(dat), sd = method_sd[dat$method])

fit_overall <- cia(
  dat,
  response = "value",
  subject = "subject",
  method = "method",
  replicate = "replicate",
  scope = "overall"
)
print(fit_overall)
summary(fit_overall)
plot(fit_overall)

fit_pairwise <- cia(
  dat,
  response = "value",
  subject = "subject",
  method = "method",
  replicate = "replicate",
  scope = "pairwise"
)
print(fit_pairwise)
summary(fit_pairwise)
plot(fit_pairwise)

}
\references{
Barnhart HX, Kosinski AS, Haber M. (2007). Assessing individual agreement.
\emph{Journal of Biopharmaceutical Statistics}, 17(4), 697-719.
\doi{10.1080/10543400701329489}

Barnhart HX, Haber M, Lokhnygina Y, Kosinski AS. (2007). Comparison of
concordance correlation coefficient and coefficient of individual agreement
in assessing agreement. \emph{Journal of Biopharmaceutical Statistics},
17(4), 721-738.

Pan Y, Gao J, Haber M, Barnhart HX. (2010). Estimation of coefficients of
individual agreement (CIA's) for quantitative and binary data using SAS and
R. \emph{Computer Methods and Programs in Biomedicine}.
}
\seealso{
\code{\link{ccc}}, \code{\link{ba}}, and \code{\link{prob_agree}}.
\code{cia()} answers the question "Are methods interchangeable for
individual subjects once within-method replicate disagreement is taken into
account?". In contrast, \code{ccc()} answers "How well do two paired
measurements agree overall, combining correlation with location/scale
agreement?".
}
\author{
Thiago de Paula Oliveira
}
