% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agreement_aliases.R
\name{agreement_aliases}
\alias{agreement_aliases}
\alias{ba}
\alias{ba_rm}
\alias{ccc_rm_reml}
\alias{ccc_rm_ustat}
\title{Short Aliases for Agreement and Repeated-Measures Methods}
\usage{
ba(
  group1,
  group2,
  two = 1.96,
  mode = 1L,
  conf_level = 0.95,
  verbose = FALSE
)

ba_rm(
  data = NULL,
  response,
  subject,
  method,
  time,
  two = 1.96,
  conf_level = 0.95,
  include_slope = FALSE,
  use_ar1 = FALSE,
  ar1_rho = NA_real_,
  max_iter = 200L,
  tol = 1e-06,
  verbose = FALSE
)

ccc_rm_reml(
  data,
  response,
  rind,
  method = NULL,
  time = NULL,
  interaction = FALSE,
  max_iter = 100,
  tol = 1e-06,
  Dmat = NULL,
  Dmat_type = c("time-avg", "typical-visit", "weighted-avg", "weighted-sq"),
  Dmat_weights = NULL,
  Dmat_rescale = TRUE,
  ci = FALSE,
  conf_level = 0.95,
  ci_mode = c("auto", "raw", "logit"),
  verbose = FALSE,
  digits = 4,
  use_message = TRUE,
  ar = c("none", "ar1"),
  ar_rho = NA_real_,
  slope = c("none", "subject", "method", "custom"),
  slope_var = NULL,
  slope_Z = NULL,
  drop_zero_cols = TRUE,
  vc_select = c("auto", "none"),
  vc_alpha = 0.05,
  vc_test_order = c("subj_time", "subj_method"),
  include_subj_method = NULL,
  include_subj_time = NULL,
  sb_zero_tol = 1e-10
)

ccc_rm_ustat(
  data,
  response,
  method,
  subject,
  time = NULL,
  Dmat = NULL,
  delta = 1,
  ci = FALSE,
  conf_level = 0.95,
  n_threads = getOption("matrixCorr.threads", 1L),
  verbose = FALSE
)
}
\arguments{
\item{group1, group2}{Numeric vectors of equal length.}

\item{two}{Positive scalar; the multiple of the standard deviation used to
define the limits of agreement.}

\item{mode}{Integer; 1 uses \code{group1 - group2}, 2 uses \code{group2 - group1}.}

\item{conf_level}{Confidence level.}

\item{verbose}{Logical; print brief progress or diagnostic output.}

\item{data}{A \code{data.frame} containing the repeated-measures dataset.}

\item{response}{Numeric response vector or column name, depending on the target method.}

\item{subject}{Subject identifier or subject column name.}

\item{method}{Method label or method column name.}

\item{time}{Replicate/time index or time column name.}

\item{include_slope}{Logical; whether to estimate proportional bias.}

\item{use_ar1}{Logical; whether to use AR(1) within-subject correlation.}

\item{ar1_rho}{AR(1) parameter.}

\item{max_iter, tol}{EM control parameters.}

\item{rind}{Character; column identifying subjects for \code{ccc_rm_reml()}.}

\item{interaction, Dmat, Dmat_type, Dmat_weights, Dmat_rescale, ci, ci_mode,
digits, use_message, ar, ar_rho, slope, slope_var, slope_Z,
drop_zero_cols, vc_select, vc_alpha, vc_test_order, include_subj_method,
include_subj_time, sb_zero_tol}{Additional arguments forwarded to
\code{ccc_lmm_reml()}.}

\item{delta}{Numeric power exponent for U-statistics distances.}

\item{n_threads}{Integer number of OpenMP threads.}
}
\value{
The same object returned by the corresponding full function.
}
\description{
Convenience aliases for selected agreement and repeated-measures
functions in \pkg{matrixCorr}. These aliases do not change computation or
outputs; they simply forward to the corresponding full function names.
}
\examples{
set.seed(1)
x <- rnorm(40)
y <- x + rnorm(40, sd = 0.5)
ba(x, y)
}
